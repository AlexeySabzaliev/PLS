# План рефакторинга и дебага (2026-09-07)

## Проблемы
1. Данные с портала охраны (КПП) не подтягиваются
2. Биллинг неверно формируется/калькулируется
3. Отчёт ФОТ не выводит операционку (зависит от биллинга)
4. Отчёт по отклонениям не работает

## Шаги
- [x] Диагностика импорта данных с портала охраны (security_*) — offline-fallback `local_db_offline`/`local_db_fallback`
- [x] Диагностика биллинга и агрегации операционки — кастомные коды маппятся в стандартные; счёт по `period_from/period_to`
- [x] Диагностика отчёта ФОТ — `test_fot_report` passed
- [x] Диагностика отчёта по отклонениям (arrival_gap) — формула подтверждена тестом
- [x] Починка найденных проблем
- [x] Проверка тестов `pytest -v` — **226 passed** (2026-09-07)

### Осталось из этой волны
- [ ] Коммит незакоммиченного набора (после `a7b873c`) — по задачам №1–№4 + прочее
- [ ] Сентябрь: наполнение данных с 2026-09-08, живая сверка ФОТ и отчёта отклонений; тарифы — на стандартных canonical-кодах (или имаппленные)
- [ ] Не коммитить диагностический мусор: `scripts/diag_out.txt`, `scripts/diag_err.txt`, `scripts/diag_billing.py`, `tests_output.txt`

### Контекст сессий
Журнал сессий — [SESSION_LOG.md](./SESSION_LOG.md). Начинать сессию с чтения этого файла и STATUS.